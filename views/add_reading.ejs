<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <style>
        /* Make table horizontally scrollable on mobile */
        .table-wrapper {
            position: relative;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }

        /* Sticky first column */
        .table-sticky th:first-child,
        .table-sticky td:first-child {
            position: sticky;
            left: 0;
            background: white;
            z-index: 1;
            border-right: 2px solid #dee2e6;
        }

        .add_row_button {
            position: sticky;
            left: 0;
            background: white;
            z-index: 1;
            border-right: 2px solid #dee2e6;
        }

        /* Compact inputs on mobile */
        @media (max-width: 768px) {
            .form-control {
                padding: 0.25rem;
                font-size: 0.875rem;
                min-width: 60px;
            }

            th,
            td {
                white-space: nowrap;
                padding: 0.5rem !important;
            }
        }

        /* Highlight active row */
        tr:focus-within {
            background-color: #f8f9fa;
        }

        /* Show horizontal scroll indicator */
        .scroll-indicator {
            display: none;
            text-align: center;
            padding: 0.5rem;
            color: #6c757d;
            background: #f8f9fa;
            border-radius: 0.25rem;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .scroll-indicator {
                display: block;
            }
        }

        body {
            padding-bottom: 20px;
        }

        .navbar {
            margin-bottom: 20px;
        }

        .button-container {
            position: sticky;
            bottom: 0;
            z-index: 2;
            background: #ffffff;
            padding: 1rem 0;
            border-top: 2px solid #dee2e6;
        }

        .button-container button {
            margin: 0.5rem;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark" aria-label="Tenth navbar example">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample08"
                aria-controls="navbarsExample08" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse justify-content-md-center" id="navbarsExample08">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/add_products">Add Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/product_selection">Estimator</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/all-products">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/estimates">Saved Estimates</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <h2 class="m-3">Add Reading</h2>
    <div class="border border-2 p-3 rounded d-flex flex-column align-items-center my-3 mx-5">
        <form class="row g-3 mx-5 my-3 d-flex align-items-center justify-content-center flex-wrap" method="POST"
            action="/add_readings">
            <div class="col-md-6">
                <input type="number" class="form-control" id="today_reading" placeholder="Today's Reading"
                    name="today_reading" required min="0" max="100000">
            </div>
            <div class="col-md-6">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>